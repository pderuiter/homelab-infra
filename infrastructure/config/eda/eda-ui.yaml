---
apiVersion: v1
kind: Service
metadata:
  name: eda-ui
  namespace: eda
  labels:
    app.kubernetes.io/name: eda
    app.kubernetes.io/component: ui
spec:
  type: ClusterIP
  ports:
    - port: 443
      targetPort: 443
      protocol: TCP
      name: https
  selector:
    app.kubernetes.io/name: eda
    app.kubernetes.io/component: ui
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: eda-ui
  namespace: eda
  labels:
    app.kubernetes.io/name: eda
    app.kubernetes.io/component: ui
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: eda
      app.kubernetes.io/component: ui
  template:
    metadata:
      labels:
        app.kubernetes.io/name: eda
        app.kubernetes.io/component: ui
    spec:
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
              - matchExpressions:
                  - key: node-role.kubernetes.io/control-plane
                    operator: DoesNotExist
      containers:
        - name: eda-ui
          image: quay.io/ansible/eda-ui:latest
          ports:
            - containerPort: 443
              name: https
          env:
            - name: EDA_SERVER
              value: "http://eda-server:8000"
            - name: EDA_WEBHOOK_SERVER
              value: "http://eda-server:8000"
            - name: EDA_WEBSOCKET_URL
              value: "ws://eda-server:8000"
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 200m
              memory: 128Mi
          livenessProbe:
            httpGet:
              path: /
              port: 443
              scheme: HTTPS
            initialDelaySeconds: 10
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /
              port: 443
              scheme: HTTPS
            initialDelaySeconds: 5
            periodSeconds: 10
