apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: health-dashboard-data
  namespace: health-dashboard
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 1Gi
