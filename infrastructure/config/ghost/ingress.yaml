---
# Ghost Ingress with TLS
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ghost
  namespace: ghost
  labels:
    app.kubernetes.io/name: ghost
    app.kubernetes.io/component: blog
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-production
    traefik.ingress.kubernetes.io/router.entrypoints: websecure
    traefik.ingress.kubernetes.io/router.tls: "true"
    # External DNS via TransIP - A record points to docker04 (Ghost migrated
    # off K8s to docker04 behind host Traefik; was Pangolin wbyc-srv-vpn01).
    external-dns.alpha.kubernetes.io/external: "true"
    external-dns.alpha.kubernetes.io/target: 85.10.201.85
spec:
  ingressClassName: traefik
  tls:
    - hosts:
        - blog.bsdserver.nl
      secretName: ghost-tls
  rules:
    - host: blog.bsdserver.nl
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: ghost
                port:
                  number: 2368
